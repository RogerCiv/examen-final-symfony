{% extends 'base.html.twig' %}

{% block title %}PokedexPokemon{% endblock %}

{% block body %}
    <h1>PokedexPokemon</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ pokedex_pokemon.id }}</td>
            </tr>
            <tr>
                <th>Nivel</th>
                <td>{{ pokedex_pokemon.nivel }}</td>
            </tr>
            <tr>
                <th>Fuerza</th>
                <td>{{ pokedex_pokemon.fuerza }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_pokedex_pokemon_index') }}">back to list</a>

    <a href="{{ path('app_pokedex_pokemon_edit', {'id': pokedex_pokemon.id}) }}">edit</a>

    {{ include('pokedex_pokemon/_delete_form.html.twig') }}
{% endblock %}
